{
  "name": "@remotebear/data",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "yarn lint --fix",
    "test": "jest --testMatch \"**/*.test.js\"",
    "test:watch": "yarn jest --testMatch \"**/*.test.js\" --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/joi": "^17.2.3",
    "jest": "^26.6.3",
    "joi": "^17.3.0"
  }
}

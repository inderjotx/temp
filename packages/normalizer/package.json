{
  "name": "@tokenjobs/normalizer",
  "version": "0.0.0",
  "main": "src/index.js",
  "private": true,
  "scripts": {
    "check-normalizations": "node scripts/check-normalizations",
    "check-normalizations:watch": "nodemon node scripts/check-normalizations",
    "print-locations-for-test-file": "node scripts/print-locations-for-test-file",
    "lint": "eslint src",
    "lint:fix": "yarn lint --fix",
    "test": "jest --testMatch \"**/*.test.js\"",
    "test:watch": "jest --testMatch \"**/*.test.js\" --watch"
  },
  "dependencies": {
    "@tokenjobs/data-api": "*",
    "@tokenjobs/types": "*",
    "@tokenjobs/scraper": "*",
    "yargs": "^16.2.0"
  },
  "devDependencies": {
    "chalks": "^0.1.5",
    "jest": "^26.6.3",
    "nodemon": "^2.0.7"
  }
}
